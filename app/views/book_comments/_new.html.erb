<%= form_with url: book_book_comments_path(book_id: book.id), model: BookComment.new, local: true, method: :post do |f| %>
  <%= f.text_area :comment , { class: "form-control"}%>
  <%= f.submit "送信", { class: "btn btn-success"}%>
<% end %>
